import 'package:flutter/material.dart';

class AppColors {
  // Base — dark navy, not purple
  static const background = Color(0xFF0D1B2A);
  static const surface = Color(0xFF182635);
  static const card = Color(0xFF1F3448);
  static const border = Color(0xFF2A4460);

  // Hero accent — gold
  static const gold = Color(0xFFF4C542);
  static const goldDark = Color(0xFFBF8E10);

  // Aliases
  static const primary = Color(0xFFF4C542);
  static const secondary = Color(0xFF4ECDC4);
  static const accent = Color(0xFFF4C542);

  // Feedback
  static const win = Color(0xFF4CAF50);
  static const lose = Color(0xFFE53935);
  static const neutral = Color(0xFF607D8B);

  // Game card solid colors
  static const colorRoulette = Color(0xFFCC2929);
  static const colorBlackjack = Color(0xFF1A5C38);
  static const colorSlots = Color(0xFFD4720A);
  static const colorTrivia = Color(0xFF1558B0);
  static const colorLeaderboard = Color(0xFF6A2FA0);
  static const colorProfile = Color(0xFF0F7A5E);
  static const colorMamadeira = Color(0xFFFF8F00); // laranja tang
  static const colorCigarro = Color(0xFFD32F2F); // vermelho vivo
  static const colorAiHost = Color(0xFF8E24AA); // roxo do "Tio Sorte"

  // Card suits
  static const suitRed = Color(0xFFE53935);
  static const suitBlack = Color(0xFF1B2B3A);

  // Roulette wheel segments — kept as solid palette
  static const rouletteColors = [
    Color(0xFFCC2929),
    Color(0xFF2E8B57),
    Color(0xFF1558B0),
    Color(0xFFD4720A),
    Color(0xFF6A2FA0),
    Color(0xFFB5152B),
    Color(0xFF0F7A5E),
    Color(0xFFBF4000),
    Color(0xFF455A64),
    Color(0xFFF4C542),
    Color(0xFF7B3F00),
    Color(0xFF1A6B8A),
  ];
}
